[package]
name = "broken-app"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "baseline"
harness = false

[profile.release]
debug = true
debug-assertions = true
incremental = false
opt-level = 3
lto = false
split-debuginfo = "packed"
strip = false